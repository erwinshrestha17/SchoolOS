import { PageState } from "../ui/page-state";

export function ParentActivityView() {
  return (
    <PageState
      tone="info"
      title="Parent activity is available in the mobile app"
      description="Parents and guardians use the SchoolOS mobile app for linked-child activity updates and protected media."
    />
  );
}
