export default function SchoolSettingsIndexPage() {
  // The settings layout (SettingsRouteFrame) renders the role-aware
  // SettingsControlCenter overview at /dashboard/settings and redirects
  // legacy ?section=/?tab= links to their canonical routes.
  return null;
}
