'use client';

import { SalaryStructureList } from '../../../../components/hr/salary-structure-list';

export default function SalaryStructuresPage() {
  return (
    <div className="animate-in fade-in duration-500">
      <SalaryStructureList />
    </div>
  );
}
