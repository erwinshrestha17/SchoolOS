# Web browser smoke tests

Phase 1B browser smoke coverage should verify authenticated navigation across dashboard, students, attendance, fees, notices, notice detail, notification bell, and global student search.

The Playwright config is available at `apps/web/playwright.config.ts`.
